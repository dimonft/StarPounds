{
  "scriptDelta" : 1,
  // % Energy regen/lockout penalty when strained.
  "energyPenalty": 0.5,
  // Base strain penalty.
  "penalty": 0.25,
  // Minimum strain amount before applying anything.
  "minimumAmount": 0.05,
  // Base strain amounts per second.
  "increaseAmount": 0.2, // Increase when straining. (Affected by effort)
  "decreaseAmount": 0.2, // Decrease when strained.
  "largeDecreaseAmount": 0.5, // Decrease when not strained.
  // Delay before strain begins to decrease.
  "decreaseDelay": 1,
  // What amount of strain to start sweating.
  "sweatAmount": 0.75,
  // Range of fullness for strain to scale across, and the amount being scaled.
  "scalingRange": [1, 3],
  "scalingFactor": 2,
  "minimumScalingFactor": 0.1,
  // 'Effort' for straining to occur.
  "effortThreshold": 0.5,
  // How many seconds/second to reduce the rumble timer by when moving and strained.
  "rumbleBonus": 3
}
